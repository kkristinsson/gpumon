# gpumon
Monitor GPU usage on nvidia dgx from taskbar in windows
# install
Create a ssh key with ssh-keygen, add to ~/.authorized_keys on spark, double click the exe. Will show green when not much utilized and red when gpu is under heavy load.
