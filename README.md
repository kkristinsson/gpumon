# gpumon
Monitor GPU usage on nvidia dgx spark from taskbar in windows. Probably works with other platforms as well.
# install
Create a ssh key with ssh-keygen, add to ~/.authorized_keys on spark, double click the exe. Will show green when not much utilized and red when gpu is under heavy load. Edit the .ini file to match your things. Installer provided.

